{
    "version": 2,
    "installCommand": "npm install",
    "builds": [
        {
            "src": "src/api/vercel.ts",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/api/vercel.ts"
        }
    ]
}